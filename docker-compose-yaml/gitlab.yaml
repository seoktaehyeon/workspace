version: '2'
services:
  gitlab:
    container_name: gitlab
    image: gitlab/gitlab-ce:latest
    restart: always
    ports:
      - 80:80
      - 443:443
    volumes:
      - /workspace/gitlab/data:/var/opt/gitlab
      - /workspace/gitlab/config:/etc/gitlab
